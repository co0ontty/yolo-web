version: '3.8'

services:
  app:
    image: vibe-coding:latest
    ports:
      - "8118:80"
      - "3100:3100"
    volumes:
      - server-data:/app/data
    restart: unless-stopped

volumes:
  server-data:
